import 'package:flutter/material.dart';
import 'package:staff_cleaner/values/screen_utils.dart';

class ListUserStaff extends StatelessWidget {
  const ListUserStaff({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [Text("list")],
    );
  }
}
